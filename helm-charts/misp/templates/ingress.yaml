{{- if .Values.misp.ingress.enabled -}}
{{- $fullName := include "misp.fullname" . -}}
{{- $svcPort := .Values.misp.service.port -}}
{{- if and .Values.misp.ingress.className (not (hasKey .Values.misp.ingress.annotations "kubernetes.io/ingress.class")) }}
  {{- $_ := set .Values.misp.ingress.annotations "kubernetes.io/ingress.class" .Values.misp.ingress.className}}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-app
  labels:
    {{- include "misp.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
  {{- with .Values.misp.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.misp.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.misp.ingress.className }}
  {{- end }}
  {{- if .Values.misp.ingress.tls }}
  tls:
    {{- range .Values.misp.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.misp.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-app
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-app
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
